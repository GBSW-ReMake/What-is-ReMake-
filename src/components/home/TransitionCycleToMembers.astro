---
---

<div class="cycle-handoff">
  <p class="cycle-handoff__line reveal" style="--i:0">People make the cycle.</p>
</div>

<style>
  .cycle-handoff {
    display: flex;
    align-items: center;
    justify-content: center;
    padding-block: var(--space-3xl);
    background: var(--color-bleed);
  }
  .cycle-handoff__line {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-2xl);
    color: var(--color-on-bleed);
    text-align: center;
    margin: 0;
  }
</style>
